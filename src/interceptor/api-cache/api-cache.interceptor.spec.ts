import { ApiCacheInterceptor } from './api-cache.interceptor';

describe('ApiCacheInterceptor', () => {
  it('should be defined', () => {
    expect(new ApiCacheInterceptor()).toBeDefined();
  });
});
